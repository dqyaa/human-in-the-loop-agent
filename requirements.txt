langgraph>=0.2.0
langchain>=0.3.0
fastapi>=0.115.0
uvicorn[standard]>=0.31.0
gradio>=5.0.0
pydantic>=2.9.0
requests>=2.32.0
python-dotenv>=1.0.0

# Optional: SQLite persistence (recommended over MemorySaver)
# langgraph-checkpoint-sqlite>=2.0.0

# Optional: LLM for better drafts (templates used by default)
# openai>=1.50.0
# anthropic>=0.40.0
